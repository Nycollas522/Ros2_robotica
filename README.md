Pasta assets com modelo do robo e scripts pedidos em aula

Para rodar script de camera com open cv reinstale o NumPy compatível com o OpenCV:
```bash
pip install "numpy<2" --force-reinstall
```

Acesse a pasta onde ficam os scripts Python:

```bash
cd /root/workshop_assets/assets/scripts
```

Execute o script desejado, por exemplo:

```bash
python3 visao.py
```

e o comando para a movimentacao em 5 pontos:

```bash
python3 waypoint.py
```
